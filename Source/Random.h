#ifndef __RANDOM__#define __RANDOM__//Seed with the time (a random seed) and return the time used to seed.unsigned long SeedRandom();//Seed with an explicit seed.void SeedRandom(int val);//Return an int in the range [0 - range).  range must be (0 - 32768).int RandZeroInt(int range);//Not used anymore --- Return an int in the range (-range/2 - range/2).  range must be (0 - 32768).//Return an int in the range [-range - range].  range must be [0 - 2^31].int RandNegInt(int range);//Return a float in the range [0 - range).float RandZeroFloat(float range);//Return a float in the range [-range - range].float RandNegFloat(float range);#endif